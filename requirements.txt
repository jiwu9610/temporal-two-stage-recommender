# Pinned to the versions the reported experiments ran under.
numpy==2.2.6
pandas==2.3.3
pyarrow==23.0.1
torch==2.11.0            # CUDA build used in experiments: 2.11.0+cu128
PyYAML==6.0.3
tqdm==4.67.3
pytest==9.0.2

# Text embeddings for the content retrieval source
sentence-transformers==5.4.1
huggingface_hub==1.10.2

# Anchor experiment only (RecBole legs live in their own venv --
# see scripts/anchor/setup_recbole_env.sh)
# recbole==1.2.1
