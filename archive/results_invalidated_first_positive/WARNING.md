INVALIDATED: every artifact below was produced by rankers TRAINED on
first-positive labels (later window positives trained as negatives) while
evaluated against all-positive ground truth. The `label_mode` field inside
the JSONs refers to evaluation only. Superseded by the `_ap` artifacts under
`results/`. Kept only as the incident's audit trail.
